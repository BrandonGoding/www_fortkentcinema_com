import './hero_section.scss';

const HeroSection = () => (
  <section className="hero-section">
    <div className="hero-overlay">
      <h1>Fort Kent Cinema</h1>
      <p className="hero-tagline">Experience the Magic of Classic Cinema</p>
    </div>
  </section>
);

export default HeroSection;